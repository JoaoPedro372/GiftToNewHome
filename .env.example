# Mercado Pago
# Dashboard → Suas integrações → Credenciais de teste / produção
# Access Token: server only (TEST-… or APP_USR-…)
MP_ACCESS_TOKEN=TEST-xxxxxxxx
# Public Key: safe for the browser (needed for credit card tokenization)
VITE_MP_PUBLIC_KEY=APP_USR-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# Dashboard → Webhooks → secret signature (optional in local dev)
MP_WEBHOOK_SECRET=your_webhook_secret

# Public app URL used for Mercado Pago notification_url
# Local card payments require HTTPS: https://localhost:8080
# Production: https://seu-projeto.vercel.app
APP_URL=https://localhost:8080

# Supabase
# 1) Create a project
# 2) Run supabase/schema.sql in the SQL editor
# 3) Project Settings → API → URL + secret / service_role key
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_secret_key
